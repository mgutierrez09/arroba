# Live Drill Gate

This is the post-M4.5 gate before starting new feature work. The M4.5 ownership points are closed; confidence now comes from live behavior across local providers, workflows, and relay-backed remote machines.

Script-level policy lives in `apps/cli/scripts/README.md`: live drills default to older Codex-capable models such as `gpt-5.2` and low effort. OpenCode Workspace Live Sync local validation currently uses the OpenCode Zen model path (`--provider-model opencode=opencode/gpt-5.2`) because the OpenAI OAuth-backed OpenCode path can fail before Chariox behavior with `Token refresh failed: 401`.

Codex model override rule: when a drill includes Codex, pass an explicit provider override such as `--provider-model codex=gpt-5.2` unless the drill is intentionally validating a different Codex model. Do not assume bare `--model gpt-5.2` is enough. Some drill helpers map bare `gpt-5.2`/`gpt-5.3` to `gpt-5.2-codex`/`gpt-5.3-codex`; ChatGPT-backed Codex accounts can reject those `*-codex` ids with HTTP 400, which presents as a stuck drill with prompt echo and no provider output.

Terminology:

- **Freeform multi-agent mode** means a normal multi-agent session with no workflow scheduler. Agents can be focused, prompted, and observed independently, but no workflow graph owns turn progression.
- **Workflow mode** means a daemon-owned workflow graph schedules node turns, validates outputs, and advances runs.
- **Remote mode** means one or more CLIs or agents connect through relay to a home daemon or to a remote machine accepting leases.

## Required Drill Matrix

1. Freeform local multi-agent:
   - Start one session.
   - Attach at least one local CLI.
   - Spawn at least one OpenCode-backed agent and one Codex-backed agent.
   - Submit targeted prompts to both agents.
   - Verify each agent completes without blocking the other, focus/state projections remain correct, and provider output is visible from the attached CLI.

2. Local workflow drills:
   - Run the existing workflow drill catalog:
     - `simple-chain`
     - `validated-increment-chain`
     - `console-increment-chain`
     - `final-run-output-chain`
     - `cyclic-final-run-output-chain`
     - `cyclic-budgeted-final-run-output-chain`
     - `cyclic-final-run-with-intermediate-output-chain`
     - `conditional-branch-subset`
     - `immediate-release-downstream`
   - Verify run status, node summaries, final output, intermediate output, console entries, provider cleanup, and session projection refresh.

3. Remote freeform multi-agent through relay:
   - Start relay, home daemon, and worker daemon.
   - Attach one local CLI directly and one remote CLI through relay.
   - Spawn local and remote agents through the home session.
   - Submit targeted prompts from both CLIs, including prompts to worker-machine agents.
   - Restart relay during the session and verify transport close/resume plus continued prompt completion.

4. Remote workflow through relay:
   - Start relay, home daemon, and worker daemon.
   - Run the workflow drill catalog with agents leased on the remote worker machine.
   - Verify workflow completion, runtime-tool handling, relay recovery, and final projection state from both local and relayed clients.

## Existing Automation

- Local freeform multi-agent: `node apps/cli/scripts/live-freeform-multi-agent-drill.mjs --providers opencode,codex`
- Local workspace live sync: `pnpm --filter @chariox/cli run workspace-live-sync:drill` for smoke, `workspace-live-sync:managed-drill` for two-target managed fanout, `workspace-live-sync:same-branch-tracked-drill` for same-branch tracked fanout, `workspace-live-sync:tracked-drill` for cross-branch bidirectional tracked fanout, and `workspace-live-sync:permission-drill` for permission gating. Add OpenCode only when provider auth is healthy.
- Local runtime MCP reattach: `node apps/cli/scripts/live-runtime-mcp-reattach-drill.mjs --providers opencode,codex` or `pnpm --filter @chariox/cli run runtime-mcp-reattach:drill`
- Local workflow: `node apps/cli/scripts/live-workflow-runtime-drill.mjs --spawn-daemon --scenario <scenario> --providers opencode,codex`
  - Add `--no-early-pass` when validating the full `immediate-release-downstream` completion path instead of only the immediate release point.
- Local shell/scriptability: `pnpm --filter @chariox/cli run shell:drill`. The drill launches an isolated local kernel with a temporary home/workspace/history/ports, runs `chariox-shell run <file>` scripts, covers shell-local state, session/agent/config/MCP/skill/workflow/provider-process/stop command families without launching real provider model turns, and verifies seeded variables, `source <file>` loading, and `--continue-on-error` line diagnostics.
- Local restart/recovery: `pnpm --filter @chariox/cli run local-restart:drill`. The drill rebuilds the local kernel, launches an isolated daemon, creates a session, spawned agent, MCP grant, skill grant, provider profile, completed transcript marker, and in-flight workflow run, kills the kernel with `SIGKILL`, then verifies the same active prompt, provider run, workflow run, node, scheduler state, transcript paging, and history search survive restart.
- Local CLI restart/reconnect: `pnpm --filter @chariox/cli run cli-restart:drill`. The drill launches the real CLI under a PTY with automation enabled, verifies it renders a connected session, stops the kernel process, verifies the CLI shows the disconnected state, restarts the kernel, and verifies the CLI reattaches to the restored session and clears the disconnected state.
- Local slice lifecycle: `pnpm --filter @chariox/cli run slice:lifecycle-drill`. The drill launches an isolated local kernel and Docker-backed Linux slices, creates a headed slice, verifies the screen endpoint, imports Codex/OpenCode/Claude auth fixtures, starts Codex device login inside the slice, verifies independent provider auth summaries across two slices, rejects reuse from the wrong worktree for session and agent launch, verifies multi-session/multi-agent reuse of one slice, and blocks deletion while agents remain attached.
- Local Computer secret input: `CARGO_TARGET_DIR=/absolute/shared/cargo-target pnpm --filter @chariox/cli run computer-secret-input:drill`. The focused protocol drill validates explicit Computer credential policy; denied and allowed user interactions through the public runtime-MCP path; exact insertion into the existing desktop focus without Chromium focus changes or clipboard use; redacted MCP, log, and Room Action surfaces; actor attribution; and the versioned remote-home credential resolution shape. Disposable canary state is always removed, while non-secret evidence is stored under `~/.codex/evidence/browser-computer-use/computer-secret-input/`.
- Local Computer clipboard: `pnpm --filter @chariox/cli run computer-clipboard:x11-drill`. The resource-bounded Docker drill reuses the existing slice image and runs the production screen helper against real Xvfb, Chromium, and `xclip`. It verifies exact empty, Unicode, whitespace, trailing-newline, repeat-read, and 256 KiB boundary behavior; forces an `xclip` write failure; rejects helper/log/evidence leakage; and proves container and plaintext temporary-file cleanup. Digest-only evidence is stored under `~/.codex/evidence/browser-computer-use/computer-clipboard-x11/`.
- Local physical Unicode keyboard regression: `pnpm --filter @chariox/cli run computer-keyboard:x11-drill`. This isolated, network-disabled Xvfb/Chromium drill calls the production typing helper repeatedly with Latin, CJK, and Cyrillic input, both with an idle and a busy renderer, including a 96-symbol run that exercises mapping recycling. It also kills a live input process group, checks that typing stops after reset, and verifies reset releases printable keys and modifiers. It compares exact text without retaining it, records only counts and mismatch positions, and removes its 768 MiB/one-CPU container on success or failure. The text helper reuses the pinned Selkies XTEST keyboard implementation without clipboard or DOM insertion; keys are paced in the helper process rather than queued with delayed execution in X11. Evidence is stored under `~/.codex/evidence/browser-computer-use/computer-keyboard-x11/`. This is a red-capable functional test, not a benchmark or a substitute for the Room/TUI end-to-end drill.
- Local slice display process faults: `pnpm --filter @chariox/cli run slice:display-fault-drill`. This current-source overlay drill uses an existing slice image without rebuilding it, disables container networking, and caps the run at one CPU, 1 GiB memory and 256 processes. It kills Selkies and proves Browser remains available plus viewer-retry recovery, then kills Chromium while the recovered streamer remains live and proves desktop lifecycle recovery restores a previously verified browser-profile checkpoint. It also exercises failed Selkies startup, explicit noVNC rollback, display-socket/listener cleanup and container removal. Structured evidence, including before/during/after resources, is stored under `~/.codex/evidence/browser-computer-use/slice-display-faults/`. The aggregate runtime resilience matrix runs this scenario only with `--include-slices`.
- Local Room Browser Controller process fault: `pnpm --filter @chariox/cli run browser-controller:fault-drill`. This runs one serial, exact kernel library scenario that SIGKILLs the controller, attributes the rejected mutation as `process_lost`, rejects the old element reference, replaces the process, preserves one tab registry and input authority, and completes one fresh action exactly once. It then crashes the replacement while one same-tab mutation is running and another is queued, requires both pre-crash actions to settle without changing the page, and proves only a newly discovered post-recovery action executes. It always passes Cargo `--lib`, uses one build job and an external shared target, records before/during/after resources, and verifies fixture controller cleanup. Evidence is stored under `~/.codex/evidence/browser-computer-use/controller-faults/` and the aggregate runtime resilience matrix includes the scenario in its default local set.
- Local relay queue saturation: `pnpm --filter @chariox/cli run relay:queue-saturation-fault-drill`. This exact relay library probe fills client, daemon-peer, event-subscriber, and peer-event queues. It requires retryable target backpressure with pending-request cleanup, isolates a slow subscription without removing a healthy subscription, keeps the daemon reader lane live, and checks the backpressure counters. The shared local Rust-fault runner keeps Cargo serial and library-only, stores its shared target outside the repo, captures before/during/after resources, and writes evidence under `~/.codex/evidence/browser-computer-use/queue-saturation-faults/`. The aggregate runtime resilience matrix includes it in the default local set.
- Local relay identity expiry: `pnpm --filter @chariox/cli run relay:identity-expiry-fault-drill`. The drill starts an exact prebuilt relay binary from the external shared Cargo target and signs the same HS256 JWT claim format used by Cloud. It connects two isolated realms, creates the short-lived token immediately before connection, and requires the relay to close only that socket at expiry. An existing healthy client must complete encrypted request and response routes through the daemon both before and after that close, while a separate metadata connection also stays live. The drill accepts a token issued 30 seconds ahead; rejects tokens that are expired, issued beyond the 60-second skew allowance, invalid, role-mismatched, or bound to another public key; and denies cross-realm routing without leaking either machine inventory. It samples resources, terminates its relay process, and writes evidence under `~/.codex/evidence/browser-computer-use/relay-token-expiry/`. The aggregate runtime resilience matrix includes it in the default local set.
- Local reconnect storm and slow viewer: `pnpm --filter @chariox/cli run runtime-resilience:reconnect-storm-drill`. The drill runs real kernel and relay binaries from the external `CARGO_TARGET_DIR` (defaulting to `~/.chariox/dev/browser-computer-use/cargo-target`), gives every simulated viewer an independent Room attachment and replay cursor, repeats concurrent event-stream reconnects, then pauses one viewer socket while the slow-output flood continues to advance. Relay health exposes the number of currently pressured subscription queues and their maximum live depth, so the drill starts its healthy probe only after it observes queued slow-lane traffic. Before that slow lane is isolated, the remaining viewers receive output, an independent kernel control request succeeds, and a real dev-stub prompt produces provider output and a completed turn within a deadline. The evidence records both the healthy-completion and later slow-lane-closure timestamps and requires observed queue pressure plus an active slow subscription throughout the healthy probe. Release binaries are the default; set `CHARIOX_RECONNECT_STORM_BUILD_PROFILE=debug` only for a focused development run. Direct runs default to 32 viewers and five cycles; the aggregate local resilience matrix uses a laptop-safe eight viewers, three cycles, and 4,096 slow-lane events, which is sufficient to close the stalled local TCP subscriber after the healthy probe. The drill enforces a 1 GiB kernel RSS ceiling, terminates all owned processes, and writes reports outside the repository under `~/.codex/evidence/browser-computer-use/reconnect-storm/`.
- Local human takeover reconnect: `pnpm --filter @chariox/cli run room:takeover-reconnect-fault-drill`. The exact kernel transport probe drops the first response after a human desktop takeover commits, then retries the same command ID from a fresh connection. The cached response must match the committed Room snapshot, the action ledger must retain one human owner and exactly one input-ownership event, and an agent Computer mutation must return `RejectedTakeover` until that human explicitly releases input. Only then may the agent mutation start. The serial Rust runner uses the shared external Cargo target, records resource samples under `~/.codex/evidence/browser-computer-use/room-takeover-reconnect/`, and verifies removal of its private fixture. The aggregate runtime resilience matrix includes it in the default local set.
- Local slice memory-pressure admission: `pnpm --filter @chariox/cli run slice:memory-pressure-admission-fault-drill`. New Linux slices default to a 2048 MiB Docker limit. A host-wide file lock serializes Docker starts across kernel processes before the admission check sums active Chariox slice limits and retains 512 MiB for the Docker engine. Existing targets reserve their actual configured limit, while any legacy unbounded target or active slice fails closed with recreation guidance. The exact kernel probe proves unsafe rejection leaves active state unchanged and admission reopens after capacity recovers. The shared local Rust-fault runner keeps Cargo serial and library-only, records before/during/after resources, and writes evidence under `~/.codex/evidence/browser-computer-use/memory-pressure-admission/`. The aggregate runtime resilience matrix includes it in the default local set.
- Local slice disk-pressure admission: `pnpm --filter @chariox/cli run slice:disk-pressure-admission-fault-drill`. Every save and backup quiesces the source before measuring it and holds one host-wide admission lock through commit and archive publication. Live capture stops the desktop, pauses the remaining container processes, and always resumes the source after success or rejection. The exact kernel integration probe drives the real live-save entry point with a mocked Docker boundary: it forces low capacity, proves rejection occurs before commit while the prior manifest remains byte-identical and the measurement helper is removed, then restores capacity and proves a replacement generation publishes. The shared local Rust-fault runner records exact source plus before/during/after resources under `~/.codex/evidence/browser-computer-use/disk-pressure-admission/`; the aggregate runtime resilience matrix includes it in the default local set.
- Local slice-save acknowledgement loss: `pnpm --filter @chariox/cli run slice:save-ack-loss-fault-drill`. The exact kernel probe dispatches `SaveSliceState` through the production transport handler and real slice-save service, with only Docker replaced by a counted executable at the external-effect boundary. It drops the first outgoing response, then retries the same command id in-process and after the persistent transport cache is reloaded. Both replies must preserve the exact saved-state ref and home-archive generation while the Docker commit count remains one; conflicting command-id reuse fails closed. The probe removes and verifies its private temp root before reporting cleanup success. The shared serial Rust-fault runner records resources and cleanup under `~/.codex/evidence/browser-computer-use/slice-save-ack-loss/`; the aggregate runtime resilience matrix includes it in the default local set.
- Local interrupted slice save: `pnpm --filter @chariox/cli run slice:save-interruption-fault-drill`. The exact kernel probe injects failures at both saved-state publication boundaries. Before manifest publication, the prior manifest and archive must remain restorable and the unpublished archive must be removed. After an atomic manifest rename with uncertain directory durability, both archive generations must remain available, and both the prior record and manifest-selected record must resolve to their respective restore inputs. The shared serial Rust-fault runner records resources and cleanup under `~/.codex/evidence/browser-computer-use/slice-save-interruption/`; the aggregate runtime resilience matrix includes it in the default local set.
- Local saved-state corruption: `pnpm --filter @chariox/cli run slice:saved-state-corruption-fault-drill`. The exact kernel probe corrupts one backup archive and requires restore validation to move it atomically out of the restorable path into a durable quarantine file before Docker image inspection or runtime replacement. A separate known-good backup must remain byte-identical and pass the complete archive, manifest, and image integrity validator. The backup directory owns the quarantine file, so normal backup deletion removes it. The shared serial Rust-fault runner records resources and cleanup under `~/.codex/evidence/browser-computer-use/saved-state-corruption/`; the aggregate runtime resilience matrix includes it in the default local set.
- Local interrupted slice restore: `pnpm --filter @chariox/cli run slice:restore-interruption-fault-drill`. The exact kernel process test starts a backup restore through the production command router, persists its transaction, creates the target replacement through the configured provisioner, and then SIGKILLs the child kernel. The parent verifies that no restored or rolled-back resolution was published before the crash. Restarting the kernel must recover the durable transaction, restore the rollback image and home archive through the production startup path, remove the partial target runtime, publish one failed rolled-back slice state, and retain no false committed restore. The external-effect boundary uses counted Docker and provisioner executables; the fixture root and child process must be absent before the probe reports success. The shared serial Rust-fault runner records resources and cleanup under `~/.codex/evidence/browser-computer-use/slice-restore-interruption/`; the aggregate runtime resilience matrix includes it in the default local set.
- Local Room Computer input end to end: `CARGO_TARGET_DIR=/absolute/shared/cargo-target pnpm --filter @chariox/cli run computer-input:room-e2e-drill`. The drill starts a local kernel, scoped relay, headed Selkies slice, slice-bound agent, direct local TUI, and relay-attached remote TUI. It proves physical move, single click, right click, double click, text-selection drag without window movement, and horizontal plus vertical scrolling through the agent's Room-authorized `slice_mouse` tool. The agent uses that same Room-authorized pointer path to focus the keyboard field before typing. The drill then proves non-US keyboard text reaches the physical X11 focus, a select-all chord replaces the field, repeated BackSpace executes exactly without losing focus, and keyboard history and terminal notices retain no text or key names. It starts real physical keyboard input and proves both explicit local-TUI cancellation and remote-TUI human takeover stop the helper, settle exactly one cancelled Action, stop further typing, reset input before reuse, and remain visible in both TUIs without retaining text. Agent and human writes reach one physical X11 clipboard through home-owned Room authority, human read requires takeover and creates no Action, takeover rejects agent mutation, and cleanup removes every owned resource. Action sequence numbers keep repeated same-kind outcomes distinct in both TUIs. `computer-input-cancellation:room-e2e-drill`, `computer-pointer:room-e2e-drill`, and `computer-clipboard:room-e2e-drill` remain aliases for this aggregate drill.
- Staging web View-tab vault slice: from `chariox-cloud`, run `node scripts/staging-vault-view-slice-drill.mjs`. The drill uses the hosted web product with the local home kernel, creates a headed slice session from the waiting-room UI, opens the View tab, asks the slice agent to create a generated vault credential and a user-entered vault credential through runtime MCP, pastes both into a browser password-field page inside the slice, captures View-tab screenshots under `chariox/.artifacts/m26-vault-view-slice/<run-id>`, and scans home/worker traces for secret, passphrase, or `vault_key` leakage. Set `CHARIOX_VAULT_VIEW_PASSPHRASE` only when the home vault may be locked; omit it when the vault is already unlocked.
  - Required evidence: the screenshot set must include the real terminal shell while the session is still launching, the headed-slice screen link in the terminal shell, the View tab before prompting, the submitted prompt, vault unlock popup when applicable, user-secret popup, and final View state. The manifest must include `diagnostics.terminalLaunch` proving `/terminal` mounted `freeform-workspace` and not `waiting-room-workspace`.
  - Required safety assertions: product text, home history, and worker history must not contain the user-entered secret, vault passphrase, `vault_key`, or helper stdout/stderr from `chariox.paste_secret_to_slice`.
  - Missing follow-up gate before declaring vault+slice A+: add a public-service registration drill that runs from the View tab, has the agent create a generated credential, register/log out/log in using only runtime MCP browser and vault tools, confirm email through the saved Gmail slice or a deterministic mail fixture, and produce a trace-friction report covering tool discovery and any runtime MCP confusion.
- Local native TUI:
  - Codex: `pnpm --filter @chariox/cli run native-tui:codex-drill`.
  - OpenCode: `pnpm --filter @chariox/cli run native-tui:opencode-drill`.
  - Permissions: `pnpm --filter @chariox/cli run native-tui:permission-drill`.
  - Provider commands: `pnpm --filter @chariox/cli run native-tui:provider-command-drill`.
  - Prompt injection: `pnpm --filter @chariox/cli run native-tui:prompt-injection-drill`.
  - Attachments: `pnpm --filter @chariox/cli run native-tui:attachment-drill`.
  - These drills launch two provider-native TUIs into one Chariox session plus one observer Chariox CLI, validate native-CLI and Chariox-CLI prompt submission in both directions, check per-agent output separation, and observe native-TUI agent badges moving from idle to working and back to idle.
- Remote native TUI same-host relay: `node apps/cli/scripts/live-remote-native-tui-drill.mjs --providers opencode,codex,claude` or `pnpm --filter @chariox/cli run native-tui:remote-drill`. This drill launches two remote/split provider-native TUIs into one Chariox session plus one observer Chariox CLI per provider, validates prompts from both Chariox and native TUIs, validates output visibility without cross-agent contamination, and observes native-TUI agent badges moving from idle to working/thinking and back to idle. Prompt attachment transfer for remote/slice native TUI is tracked separately in `docs/M14B_NATIVE_TUI_VALIDATION_PLAN.md`.
- Remote native TUI standard home-worker: `node apps/cli/scripts/live-remote-native-tui-drill.mjs --standard-home-worker --providers opencode,codex,claude`. Codex/OpenCode support `--include-permissions` and `--include-attachments` with both native-origin and Chariox-origin validation. Claude uses `--remote-rendered` PTY transport in this topology; prompt/turn, image attachment, and permission validation pass in both native-origin and Chariox-origin directions in same-host relay mode.
- Remote native TUI actual Hetzner worker: `node apps/cli/scripts/live-remote-native-tui-drill.mjs --hetzner-worker --providers opencode,codex,claude --include-permissions --include-attachments`. This launches the relay and worker kernel on the configured Hetzner host, connects local native TUIs to the home kernel, and validates worker-owned provider execution. Codex/OpenCode pass prompt/turns, provider permissions, prompt attachments, cross-agent separation, and badge transitions. Claude requires a provider-supported `claude setup-token` credential stored in the Chariox encrypted vault and injected as `CLAUDE_CODE_OAUTH_TOKEN`. The drill must not read or write macOS Keychain.
- Remote native TUI home-managed local Docker slice: `node apps/cli/scripts/live-remote-native-tui-drill.mjs --home-managed-slice-local-docker --providers opencode,codex,claude --include-permissions --include-attachments`. This creates a slice through the home kernel, launches two provider-native TUIs plus one Chariox observer against the home session, places provider execution on the slice worker with `slice_ref`, validates prompt/turns, provider permissions, prompt attachments, cross-agent separation, and badge transitions, then deletes the slice on success. The drill rebuilds the local slice image by default so the worker kernel matches the checked-out source; set `CHARIOX_NATIVE_TUI_SLICE_BUILD_IMAGE=auto` only when intentionally reusing a known-current image.
- Remote native TUI MCP/skills: add `--include-mcp-skills` to the same-host, standard home-worker, or home-managed local Docker slice commands. The drill validates agent-scoped MCP rendering and same-turn skill requests for native TUI provider runs. Claude hidden skill context is delivered through the `UserPromptSubmit` hook `additionalContext` response and must not appear in the visible native TUI transcript.
- Local Git observation: `pnpm --filter @chariox/cli run git-observation:drill`. The drill launches an isolated local kernel and dev-stub provider in a temporary Git repo, waits until the prompt has been dispatched, commits a changed file during the agent turn, completes the prompt, then verifies operational history contains a `git_commit_detected` event searchable by commit subject/path/provider/model/prompt attribution.
- Remote Git observation: `pnpm --filter @chariox/cli run remote-git-observation:drill`. The drill launches isolated relay/home/worker kernels, spawns a remote dev-stub agent in a worker Git repo, waits for the remote prompt to dispatch, commits a changed file on the worker worktree during the remote turn, completes through home, then verifies home operational history contains a `git_commit_detected` event with home agent/prompt ids and worker machine/repo/worktree metadata.
- Shell prompt submission: covered by shared executor tests for no-wait prompt id output, `context` busy-state refresh, and `--wait --show-summary` prompt-id blob rendering. Provider-backed prompt completion remains covered by freeform/provider live drills.
- Embedded workflow shell: `pnpm --filter @chariox/cli run embedded-shell:drill`. The drill launches the real CLI under a PTY, controls it through `--automation-socket`, runs `source <file>` in the workflow-pane shell, and asserts structured CLI snapshots for workflow screen activation, selected workflow, graph counts, and shell transcript updates.
- Local MCP/skill management: `node apps/cli/scripts/live-mcp-skill-drill.mjs --providers opencode,codex` or `pnpm --filter @chariox/cli run mcp-skill:drill`. The drill installs real MCPs, installs at least one public GitHub skill into `.chariox/skills` when the network is available, verifies per-agent grants, verifies same-turn skill request bodies, and cleans its isolated workspace/daemon/session artifacts on success. Pass `--live-mcp-use` to require actual provider-native Playwright tool calls for both user-triggered MCP grant activation and agent-triggered `request_extension` activation with automatic continuation.
- Local script extensions: `pnpm --filter @chariox/cli run script-extension:drill`. The drill launches an isolated daemon, registers an external Python environment, validates and registers a vector-lookup script with `run`/`test_run`, lists environments/scripts, grants the script to an agent, and verifies `extension_grants`.
- Remote freeform multi-agent: `node apps/cli/scripts/live-remote-multi-agent-relay-drill.mjs --providers opencode,codex`
- Remote workspace live sync smoke/full: `pnpm --filter @chariox/cli run workspace-live-sync:remote-drill` for full smoke, `workspace-live-sync:remote-managed-drill` for two-target managed relay fanout, `workspace-live-sync:remote-tracked-drill` for two-target bidirectional tracked relay fanout, and `workspace-live-sync:remote-permission-drill` for remote permission gating. Add OpenCode only when provider auth is healthy.
- Remote workspace live sync actual Hetzner worker: add `--hetzner-worker` to run the relay and worker kernel on the configured Hetzner host while the home kernel remains local. Use `--mode managed` or `--mode tracked`; the drill mirrors initialized fixture worktrees to the remote worker before spawning the leased provider agent.
- Remote workspace live sync permissions: `node apps/cli/scripts/live-remote-workspace-live-sync-permission-drill.mjs --provider codex --provider-model codex=gpt-5.2` validates that a leased worker's workspace live sync mutation request surfaces as a home-kernel permission interaction and resumes after approval. Add `--hetzner-worker` to run the relay and worker kernel on the configured Hetzner host; the wrapper mirrors the permission fixture workspace to the worker before spawning the leased provider agent.
- Remote skill management: `node apps/cli/scripts/live-remote-skill-drill.mjs --provider opencode --model opencode/gpt-5.2 --effort low` and `node apps/cli/scripts/live-remote-skill-drill.mjs --provider codex --model gpt-5.2 --effort low`. The drill verifies local-only grants do not materialize on the worker, local-to-remote move synchronizes existing skill grants, remote-first grant-time materialization works, prompt-time synchronization repairs a removed worker copy, same-turn remote `request_extension` materializes the skill, worker-local `materialized_root` injection works, and providers use a synchronized skill asset through workspace live sync.
- Remote MCP management: `node apps/cli/scripts/live-remote-mcp-drill.mjs --provider opencode --model opencode/gpt-5.2 --effort low` and `node apps/cli/scripts/live-remote-mcp-drill.mjs --provider codex --model gpt-5.2 --effort low`. V1 behavior requires the worker to already have the matching MCP definition installed in its project/user Chariox registry, and missing/mismatched/missing-command/missing-env cases fail fast before remote provider launch/prompt. Pass `--live-mcp-use` to additionally require a provider-native remote Playwright/browser MCP tool call on the worker and a workspace live sync marker write.
- Remote restart/recovery: `pnpm --filter @chariox/cli run remote-restart:drill`. The drill launches isolated relay, home, and worker kernels, spawns a remote dev-stub agent, verifies baseline prompting, restarts home, restarts worker, restarts both, and requires the home kernel to restore the remote agent and refresh stale worker leases. Use `--keep-artifacts-on-failure` to preserve the isolated logs. After restarts, validate through the home kernel's remote-machine/kernel APIs rather than direct relay alias probes; the product path is what must recover.
- Remote workflow: `node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario <scenario> --providers opencode,codex`
- Lower-level relay runtime: `node apps/cli/scripts/live-relay-runtime-drill.mjs`
- Lower-level remote machine runtime: `node apps/cli/scripts/live-remote-machine-runtime-drill.mjs`

## Exit Criteria

- Every required drill either passes or has a filed blocker with repro command, observed failure, and owner.
- `cargo check --manifest-path apps/kernel/Cargo.toml` remains clean.
- Daemon lib, runtime integration, websocket integration, relay-client, and bin tests remain green after any drill fixes.
- Docs reflect the final drill results before new tasks begin.

Workspace identity note: workspace live sync captures repo/branch/head identity for each provider run. If a drill changes `HEAD`, branch, or repo identity while another workspace live sync run is active, the kernel may reject the next tool call with `workspace_identity_changed`. That is expected protection, not a merge conflict; rerun the drill from a stable workspace identity.

## Current Results

- Freeform local multi-agent: **pass** with `opencode,codex` after the router bootstrap lock fix.
- Local workspace live sync: **Codex and OpenCode Zen pass** on 2026-05-29. Codex uses `gpt-5.2`; OpenCode uses `opencode/gpt-5.2` because the OpenAI OAuth-backed OpenCode path can fail before Chariox behavior with `Token refresh failed: 401`. The managed drill validates reads, text/opaque write/edit/move/delete fanout, direct-write suppression, deterministic same-area collision handling, stale non-overlap external-change rebase, and stale overlap external-change rejection. The tracked drill validates two-target fanout, cross-branch binding, bidirectional target-origin sync, `.charioxignore`, outside-turn ignore, no commits, conflict reporting, and resolver convergence.
- Local Workspace Live Sync matrix: **Codex pass** on 2026-06-04 with `gpt-5.5`, after the ChatGPT-backed Codex account rejected the old `gpt-5.2` override before Chariox behavior. `local-off-codex`, `local-managed-codex`, `local-tracked-codex`, and `local-permission-codex` passed through `live-workspace-live-sync-matrix-drill.mjs`; the permission drill also verified that direct writes outside the selected repo remain allowed and unsynced.
- Same-host remote Workspace Live Sync matrix: **Codex pass** on 2026-06-04 with `gpt-5.5`. `remote-managed-codex`, `remote-tracked-codex`, and `remote-permission-codex` passed through `live-workspace-live-sync-matrix-drill.mjs --include-remote`, covering leased worker relay fanout, tracked conflict/resolution status, and remote permission approval; the permission drill again verified direct writes outside the selected repo remain allowed and unsynced.
- Hetzner Workspace Live Sync matrix: **Codex pass** on 2026-06-04 with `gpt-5.5`. `hetzner-managed-codex`, `hetzner-tracked-codex`, and `hetzner-permission-codex` passed through `live-workspace-live-sync-matrix-drill.mjs --include-hetzner`, covering the actual remote worker, fixture mirroring, managed fanout, tracked bidirectional fanout/conflict resolution, home-kernel permission approval, and direct writes outside the selected repo remaining allowed and unsynced.
- Local workspace live sync v2 matrix: **Codex pass** on 2026-05-29; revalidated end-to-end locally after Scalingo drills were deferred. Direct local and local-relay managed passed with two Git-worktree targets, text/binary add/update/delete/move, collision serialization, non-overlap rebase, overlap rejection, direct-write suppression, and runtime summary notices; direct local same-branch tracked and cross-branch tracked passed with two targets, bidirectional fanout, `.charioxignore`, outside-turn ignore, no commits, conflict reporting, runtime summary/conflict notices, resolver convergence, and explicit branch/workspace-link binding. Local and local-relay permission drills surfaced the workspace-live-sync permission interaction, approved it, and verified the gated write. Shell drill coverage now initializes its isolated workspace as a Git worktree root before `workspace sync link`. Kernel tests pin partial apply failure handling so one bad path result does not abort later path applies; Rust workspace-live-sync filtering also revalidated collab fanout, remote membership checks, workspace-link rejection for non-Git-worktree targets, workspace-link enrollment notices, forwarded target-session notices, `.charioxignore`, force exclusions, tracked snapshots, and conflict/rebase behavior.
- Remote workspace live sync actual Hetzner worker: **Codex pass** on 2026-05-29. Revalidated after fast-forwarding `/tmp/chariox-native-remote-validate` to current HEAD `e50e6731caaa88a32af78db2ff19c1aab5da1445` and rebuilding the remote kernel and relay with Cargo 1.95. `node apps/cli/scripts/live-remote-workspace-live-sync-drill.mjs --provider codex --provider-model codex=gpt-5.2 --mode tracked --tracked-target-count 2 --tracked-bidirectional --target-branch hetzner-live-sync-e50e673-tracked --full --hetzner-worker --timeout-ms 1000000` passed with remote relay, remote worker kernel, remote Codex auth, fixture mirroring, two targets, explicit cross-branch binding, bidirectional propagation, text/binary add/update/delete/rename, dirty-target rebase/conflict, runtime summary/conflict notices, `.charioxignore`, outside-turn ignore, no commits, conflict status, and resolver convergence. `node apps/cli/scripts/live-remote-workspace-live-sync-drill.mjs --provider codex --provider-model codex=gpt-5.2 --mode managed --managed-target-count 2 --full --hetzner-worker --timeout-ms 1000000` passed with two targets, managed tool writes, opaque move/delete, direct-write suppression, runtime summary notices, collision serialization, non-overlap rebase, and overlap rejection. `node apps/cli/scripts/live-remote-workspace-live-sync-permission-drill.mjs --provider codex --provider-model codex=gpt-5.2 --hetzner-worker --timeout-ms 360000` passed after surfacing and approving the home-kernel workspace live sync permission interaction.
- Local workspace live sync v2 OpenCode: **pass with OpenCode Zen** on 2026-05-29. `opencode/gpt-5.2` passed full managed mode with two targets and full tracked mode with two cross-branch targets plus bidirectional fanout. The run covered `.charioxignore`, outside-turn ignore, no commits, dirty-target rebase/conflict, resolver convergence, managed direct-write suppression, deterministic overlap conflict, and external rebase/rejection checks. Direct OpenCode with `openai/gpt-5.2` still fails before Chariox behavior with `Token refresh failed: 401`; that is an OpenAI OAuth path issue, not a Workspace Live Sync blocker.
- Hosted Scalingo relay with OpenCode Zen: **pass** on 2026-05-29. The hosted terminal-pairing path was forced to `opencode/gpt-5.2` and passed through the staging Cloud API, hosted relay, local TUI, and Hetzner orphan TUI. The same validation pass covered remote CLI and token rotation; separate reruns passed hosted second-kernel worker leasing and hosted multi-user invite/join after a combined-run second-kernel WebSocket reset.
- Workspace live sync client UX: **pass** on 2026-05-28 for local tests. `pnpm --filter @chariox/kernel-client test`, `pnpm --filter @chariox/cli test`, `swift test --package-path apps/ios/CharioxPackage`, and `/Users/miguel/chariox-cloud pnpm test` passed, covering shell `workspace sync`, TUI `/workspace sync`, `/config workspace-live-sync`, command center entries, footer/status refresh, iOS protocol decoding, iOS slash commands, and Cloud web request-shape, side-panel controls, mode clicks, enrollment, workspace state, and workspace context coverage. Hosted Scalingo relay coverage is tracked in the hosted result above.
- Local runtime MCP reattach: **pass** with `opencode,codex`. On 2026-04-18, `node apps/cli/scripts/live-runtime-mcp-reattach-drill.mjs --providers opencode,codex --provider-model opencode=opencode/gpt-5.2 --provider-model codex=gpt-5.2 --timeout-ms 360000 --poll-ms 1000` passed in ~36s. The drill warms provider catalog endpoints before workspace live sync launch, detaches and reattaches the CLI, and verifies each provider uses Chariox runtime MCP `list_extensions` plus `read_artifact` both before and after reattach.
- Local script extensions through actual agents: **pass** with `codex,opencode,claude`. On 2026-05-19, `pnpm --filter @chariox/cli run script-extension-agent:drill -- --providers codex,opencode,claude --provider-model codex=gpt-5.2 --provider-model opencode=opencode/gpt-5.2 --provider-model claude=sonnet --timeout-ms 420000 --poll-ms 1000` passed. The drill registered one Python script and one TypeScript script, granted both to each actual provider agent, required fixed-input script calls, verified per-run hidden tokens returned by plain `run` return values, and verified each agent wrote the observed Python/TypeScript values through workspace live sync.
- Local native TUI: **pass**. On 2026-05-13, `pnpm --filter @chariox/cli run native-tui:codex-drill` passed with two provider-native Codex TUIs and one observer Chariox CLI in the same Chariox session; `pnpm --filter @chariox/cli run native-tui:opencode-drill` passed with the same topology for OpenCode. Both validated provider-TUI prompts and Chariox-CLI prompts for both native agents, no cross-agent marker contamination, and badge transitions `IDLE -> THINKING/working -> IDLE`.
- Local native TUI permissions: **pass**. On 2026-05-13, `pnpm --filter @chariox/cli run native-tui:permission-drill` passed for Codex and OpenCode. Both providers surfaced shell approval prompts in the observer Chariox CLI for native-TUI-origin prompts and Chariox-CLI-origin prompts; approving from Chariox CLI cleared the interaction and produced a completed provider-tool history event from the provider server.
- Local native TUI provider commands: **pass**. On 2026-05-13, `pnpm --filter @chariox/cli run native-tui:provider-command-drill` passed for Codex and OpenCode. Codex forwarded a native-client `thread/shellCommand` JSON-RPC command through the native proxy to the Codex app-server and verified server-side execution. OpenCode forwarded a custom provider command through `/session/{id}/command` to the OpenCode server and verified the resulting provider turn appeared in Chariox history. Chariox CLI provider-command submission remains intentionally unsupported in this slice.
- Local native TUI prompt injection: **pass**. On 2026-05-13, `pnpm --filter @chariox/cli run native-tui:prompt-injection-drill` passed for Codex and OpenCode after rebuilding `apps/kernel`. Native-TUI provider runs now send Chariox runtime/native-permission instructions to the provider server inside a marked hidden block, and the native proxies redact that block from provider-TUI-facing Codex JSON-RPC and OpenCode HTTP/SSE traffic.
- Local native TUI attachments: **pass**. On 2026-05-13, `pnpm --filter @chariox/cli run native-tui:attachment-drill` passed for Codex and OpenCode. Codex now extracts native `image`/`localImage` turn parts into Chariox prompt attachments, and both providers forward attachment-bearing native-TUI-origin and Chariox-origin prompts to the provider server. The drill validates attachment metadata crossing the proxy/kernel boundary in both directions; it does not validate provider-specific semantic interpretation of file contents.
- Remote native TUI same-host relay: **pass**. On 2026-05-14, `node apps/cli/scripts/live-remote-native-tui-drill.mjs --providers opencode,codex,claude --keep-artifacts-on-failure` passed. For OpenCode and Codex, the provider server is launched through the target kernel-owned provider run while the native TUI attaches through the relay path. For Claude Code, the target kernel owns the native TUI PTY and the local client streams and controls it through relay. All three providers passed two native agents plus one Chariox observer CLI in one session, Chariox-origin and native-origin prompts, no cross-agent marker contamination, and badge transitions `IDLE -> THINKING/working -> IDLE`. This does not validate prompt attachment transfer.
- Remote native TUI actual Hetzner worker: **pass**. On 2026-05-15, `node apps/cli/scripts/live-remote-native-tui-drill.mjs --hetzner-worker --providers opencode --include-permissions --include-attachments`, the same command for `codex`, and the same command for `claude` passed against the Hetzner worker. All three providers validated two native TUIs plus one Chariox observer in one session, native-origin and Chariox-origin prompts, worker-owned provider execution, provider permissions, prompt attachments, no cross-agent contamination, and badge transitions back to idle. Claude required exporting the local macOS `Claude Code-credentials` Keychain payload to the worker's Linux `~/.claude/.credentials.json` before the extended drill.
- Remote native TUI home-managed local Docker slice: **pass**. On 2026-05-15, `node apps/cli/scripts/live-remote-native-tui-drill.mjs --home-managed-slice-local-docker --providers opencode --include-permissions --include-attachments`, the same command for `codex`, and the same command for `claude` passed. All three providers validated two native TUIs plus one Chariox observer in one home session, slice-worker provider execution, native-origin and Chariox-origin prompts, provider permissions, prompt attachments, no cross-agent contamination, and badge transitions back to idle. Claude required Docker slice auth import to copy the Keychain credential payload into Linux credentials and mark `/workspace` trusted.
- Local shell/scriptability: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run shell:drill` passed against an isolated local kernel. The drill created a standalone shell-attached session, spawned dev-stub agents, exercised config/MCP/skill/workflow graph/workflow config/watchdog/queue/provider-process/stop command families, and verified script seed variables, nested `source <file>` loading, and `--continue-on-error` handling for TUI-only, no-active-prompt, and missing-relay failures. Shared executor tests also cover no-wait `prompt` id output, `context` busy-state refresh, and wait/show prompt-id blob rendering.
- Embedded workflow shell: **automation pass, runtime provider-stub-limited**. On 2026-04-20, `pnpm --filter @chariox/cli run embedded-shell:drill` passed against an isolated kernel. The drill launched the real CLI under a PTY, controlled it through `--automation-socket`, switched to the workflow screen, ran `source embedded-flow.chariox` in the embedded shell, and verified the selected workflow updated to `nodes=2`, `edges=1`, `endpoints=1` with shell transcript entries preserved. Running provider-backed workflow turns is still covered by the workflow runtime drill suite; the embedded shell drill intentionally validates CLI integration and graph/source state without model-dependent output.
- Multi-user scoped relay workflow: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run multi-user-workflow:drill` passed against an isolated scoped relay and kernel with three distinct relay callers. The drill verified relay-created sessions inherit the caller owner, session invites add multiple members, each user sees only their own agents/providers in freeform projections, users can add only their own agents as workflow nodes, cross-owner edges are allowed when incident to the caller's node, unrelated users cannot remove edges, stale workflow revisions are rejected, endpoint invocation is owner-only, incident edge removal is allowed, and non-owner node instructions are redacted.
- Multi-user two-CLI workflow collaboration: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run multi-user-cli-workflow:drill` passed with two real PTY-hosted CLIs connected through scoped relay to one shared session. The drill verified user-2 can attach to a shared session without auto-launching a provider run against user-1's hidden agent, user-owned node creation from each CLI, cross-owner edge add/remove live-updating both workflow screens without manual refresh, endpoint creation live-updating both screens, non-owner endpoint invocation denial, owner endpoint invocation, and workflow run visibility from both CLIs.
- Multi-user freeform scoped relay: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run multi-user-freeform-relay:drill` passed against an isolated scoped relay and kernel. The drill verified two users share one freeform session, each user sees only their own agents outside workflow mode, prompt submission succeeds for owned agents, prompt submission to another user's agent is rejected, and session-state projection redacts other-user agents.
- Cloud multi-user bridge: **covered for hosted session entry and workflow authorization hardening**. On 2026-04-21, command/unit coverage passed for cloud invite create/accept and shell command wiring, the cloud repo Prisma smoke drill passed two-cloud-user invite/member/collaborator plus session-scoped token issuance/denial, and `pnpm --filter @chariox/cli run cloud-relay:drill` passed a three-cloud-user hosted relay drill using cloud-issued session-scoped relay tokens for the M6.5 workflow/redaction assertions: per-user agent visibility, owner-only workflow node creation, endpoint owner execution, cross-owner edge edits involving caller nodes, unrelated edge removal denial, stale workflow revision rejection, and other-user instruction redaction.
- Local restart/recovery: **pass**. On 2026-07-13, both `pnpm --filter @chariox/cli run local-restart:drill` and `node apps/cli/scripts/live-local-restart-persistence-drill.mjs` passed against a freshly rebuilt isolated kernel. Each killed the kernel with `SIGKILL` during an active workflow turn and verified the same prompt id, provider relaunch configuration and durable provider session id where present, running workflow/node/scheduler state, grants, transcript paging, and operational recall survived restart without an interruption failure. Provider-run ids are process-local and may be regenerated during relaunch. On 2026-09-05, the persistence drill was revalidated after moving all disposable runtime state under `~/.chariox/dev`, evidence under `~/.codex/evidence/browser-computer-use`, and fallback builds into the shared browser/computer-use Cargo target. Its evidence matrix recorded all 15 recovery checks and both cleanup checks as passing. The drills cleaned their isolated process, port, home, history, and runtime roots after success.
- Local CLI restart/reconnect: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run cli-restart:drill` passed against a real PTY-hosted CLI and isolated kernel. The drill verified the CLI shows `Lost connection to the Chariox kernel.` after the daemon stops, then after daemon restart it reattaches to the restored session, resubscribes to kernel events with a fresh attachment, clears the disconnected state, and shows the restored agent.
- Local slice lifecycle: **pass**. On 2026-05-30, `pnpm --filter @chariox/cli run slice:lifecycle-drill` passed against an isolated kernel and local Docker slice runtime, and was rerun successfully after the web slice UX fixes. It validated headed slice creation, noVNC screen endpoint projection, provider auth fixture import for Codex/OpenCode/Claude, Codex device-login start inside the slice, independent Codex account summaries across two slices, session and agent reuse rejection for a slice from another worktree, multi-session/multi-agent reuse on the same worktree, and active-agent delete blocking.
- Remote slice lifecycle actual Hetzner host: **pass**. On 2026-05-30, after fast-forwarding `/tmp/chariox-native-remote-validate` to `e3fd2837`, installing current Rust through rustup, adding swap for the 3.7 GiB validation host, and rebuilding the Linux slice Docker image, `pnpm --filter @chariox/cli run slice:lifecycle-drill` passed on the Hetzner machine. The drill validated headed slice creation, worker-kernel discovery, noVNC screen endpoint projection, provider auth fixture import for Codex/OpenCode/Claude, Codex device-login start inside the slice, independent Codex account summaries across slices, same-worktree multi-session/multi-agent reuse, and wrong-worktree reuse rejection for both session and agent launch. The rerun also proved `state.path` home expansion for kernel event counters after the Linux bind-mount permission failure fixed in `e3fd2837`.
- Local Git observation: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run git-observation:drill` passed against an isolated kernel, dev-stub agent, and temporary Git repo. The drill committed `feature.txt` during a dispatched local agent turn, completed the prompt, and verified operational history recorded `git_commit_detected` with the commit SHA, changed path, agent id, prompt id, provider/model metadata, and searchable commit subject/path text.
- Remote Git observation: **pass**. On 2026-04-20, `pnpm --filter @chariox/cli run remote-git-observation:drill` passed against isolated relay/home/worker kernels. The drill spawned a remote dev-stub agent in a worker Git repo, committed `feature.txt` during the remote turn, completed the prompt through home, and verified home operational history recorded `git_commit_detected` with the commit SHA, changed path, home agent id, home prompt id, provider/model metadata, worker machine id, and worker `repo_root`/`worktree_path`.
- Remote restart/recovery: **pass**. On 2026-07-13, `node apps/cli/scripts/live-remote-restart-drill.mjs --keep-artifacts-on-failure` and its `same-host-remote-worker-restart` resilience-matrix scenario passed against isolated relay/home/worker kernels. The baseline and all three restart prompts completed; home-only restart preserved the original lease, while worker-only and both-kernel restart each refreshed to one new leased-agent id. This run caught and fixed orphan durable prompt events from ephemeral worker backing sessions, then passed twice with cleanup.
- Remote workspace live sync: **Codex and OpenCode Zen same-host relay pass** after leased runs require workspace live sync. The full managed drill covers text read/write/edit/apply-patch/move/delete, opaque whole-file write/read/move/delete, direct-write blocking, same-area collision serialization, stale non-overlap external-change rebase, and stale overlap external-change rejection. The tracked relay drill covers two cross-branch targets, bidirectional fanout, `.charioxignore`, outside-turn ignore, no commits, conflict reporting, and resolver convergence. OpenCode remote/staging reruns should use `opencode/gpt-5.2` rather than the stale OpenAI OAuth-backed model path.
- Workspace live sync permission parity: **Codex local, Codex same-host remote, OpenCode Zen local, and OpenCode Zen same-host remote pass**. On 2026-06-04, OpenCode Zen passed locally and through the same-host relay after unfenced managed OpenCode runs exposed provider-native bash while rejecting native permission requests that touch protected synced roots. On 2026-06-11, `node apps/cli/scripts/live-workspace-live-sync-permission-drill.mjs --provider codex --provider-model codex=gpt-5.5 --timeout-ms 240000` and `node apps/cli/scripts/live-workspace-live-sync-permission-drill.mjs --provider opencode --provider-model opencode=opencode/gpt-5.2 --timeout-ms 700000` both passed locally; `node apps/cli/scripts/live-remote-workspace-live-sync-permission-drill.mjs --provider codex --provider-model codex=gpt-5.5` passed through the same-host relay/leased-worker path. The drill writes the selected repo through Chariox Workspace Live Sync, then writes a separate Git repo outside the synced root through provider-native tools; Codex completed the outside-repo write directly, and OpenCode surfaced `external_directory` plus `bash` approvals before completing it.
- Workspace live sync OpenCode matrix: **local and same-host remote pass** on 2026-06-04 with `node apps/cli/scripts/live-workspace-live-sync-matrix-drill.mjs --include-opencode --include-remote --only local-managed-opencode,local-tracked-opencode,local-permission-opencode,remote-managed-opencode,remote-tracked-opencode,remote-permission-opencode --continue-on-failure`. Results: local managed 150s, local tracked 204s, local permission 25s, remote managed 188s, remote tracked 103s, and remote permission 61s. This revalidated two-target managed fanout, tracked cross-branch bidirectional fanout, `.charioxignore`, outside-turn exclusion, no commits, conflict reporting/resolver convergence, remote managed direct-write suppression, and local/remote outside-repo provider-native bash writes.
- Hetzner Workspace Live Sync OpenCode matrix: **pass with OpenCode Zen** on 2026-06-13 after rebuilding `/tmp/chariox-native-remote-validate` to `6dc7f481`. `node apps/cli/scripts/live-workspace-live-sync-matrix-drill.mjs --include-hetzner --include-opencode --only hetzner-managed-opencode,hetzner-tracked-opencode,hetzner-permission-opencode --continue-on-failure` passed with managed as the expected Linux unsupported fast-fail (33ms), tracked bidirectional fanout (135s), and permission gating (79s). The run covered actual Hetzner worker relay, tracked two-target fanout, `.charioxignore`, no commits, conflict/resolver convergence, home-kernel WLS approval, provider-native outside-repo bash approvals, and direct outside-repo writes remaining allowed and unsynced. An earlier permission rerun exposed a stale-worker protocol mismatch (`missing field metadata`); relay peer protocol version guard now rejects old workers before leased-agent spawn with an upgrade/restart action.
- Workspace live sync outside-repo revalidation: **pass**. On 2026-06-13, after protocol `139`, `node apps/cli/scripts/live-workspace-live-sync-matrix-drill.mjs --include-opencode --only local-permission-codex,local-permission-opencode --continue-on-failure` passed with Codex and OpenCode Zen. Results: Codex permission 32s and OpenCode permission 34s. Both scenarios proved the selected synced repo is mediated by Workspace Live Sync while a separate outside Git repo remains writable through provider-native tools/approvals.
- Same-host remote Workspace Live Sync revalidation: **pass**. On 2026-06-13, after protocol `139`, `node apps/cli/scripts/live-workspace-live-sync-matrix-drill.mjs --include-remote --include-opencode --only remote-permission-codex,remote-permission-opencode --continue-on-failure` passed with Codex and OpenCode Zen. Results: Codex remote permission 65s and OpenCode remote permission 61s, covering leased worker relay permission approval and direct writes to a separate outside Git repo remaining allowed and unsynced. Earlier the same day, protocol-`138` OpenCode managed/tracked/permission same-host relay scenarios also passed with remote managed 253s, remote tracked 161s, and remote permission 48s.
- Hetzner Workspace Live Sync outside-repo revalidation: **pass**. On 2026-06-13, after protocol `139`, `node apps/cli/scripts/live-workspace-live-sync-matrix-drill.mjs --include-hetzner --include-opencode --only hetzner-permission-codex,hetzner-permission-opencode --continue-on-failure` passed `hetzner-permission-codex` in 70s, proving tracked WLS approval and outside-repo provider-native writes on the actual worker. A later focused rerun after safe Hetzner relay cleanup hardening, `node apps/cli/scripts/live-workspace-live-sync-matrix-drill.mjs --include-hetzner --include-opencode --only hetzner-permission-opencode --continue-on-failure`, passed `hetzner-permission-opencode` in 55s. The OpenCode run approved the home-kernel Workspace Live Sync interaction, then approved provider-native `external_directory` and `bash` prompts for the separate outside Git repo and wrote the outside file without syncing it.
- Hosted Cloud tracked Workspace Live Sync: **pass with OpenCode Zen** on 2026-06-13. From `chariox-cloud`, `pnpm run smoke:hosted-oss-drill -- --second-kernel --multi-user --tracked-workspace-live-sync --tracked-provider opencode --tracked-model gpt-5.2` passed against `https://chariox-cloud-staging.osc-fr1.scalingo.io` and hosted relay `wss://195.201.123.115.sslip.io`. The combined staging run covered Cloud second-kernel pairing, multi-user collab home-extension authorization/revoke/request denial, hosted remote worker launch, tracked bidirectional Workspace Live Sync with `.charioxignore`, no commits, conflict reporting/resolver convergence, and the final two-ready-target status. Passing `--tracked-model opencode/gpt-5.2` failed before Chariox behavior because this hosted wrapper maps it to `--provider-model opencode=<model>` and OpenCode expects bare model id `gpt-5.2` on that API path.
- Local workflow catalog: **pass** with spawned local daemon and `opencode,codex`.
  - `simple-chain`
  - `validated-increment-chain`
  - `console-increment-chain`
  - `final-run-output-chain`
  - `cyclic-final-run-output-chain`
  - `cyclic-budgeted-final-run-output-chain`
  - `cyclic-final-run-with-intermediate-output-chain`
  - `conditional-branch-subset`: router node emits structured `workflow_handoffs`, only the selected even-value branch runs, and the excluded branch is not invoked.
  - `immediate-release-downstream`: producer submits validated intermediate output, downstream starts before producer completion, consumes `{"value":1842}`, and submits final workflow output `{"value":1843}`. Full completion is verified with both provider orders using `--no-early-pass`.
- Remote freeform relay: **pass** with `opencode,codex`, direct local client plus relayed client, local sidecar agents plus worker-machine leased agents, relay restart, transport close/resume, and post-reconnect prompt completion.
- Remote workflow relay: **pass** with `opencode,codex`, relay, home daemon, worker daemon, remote worker-machine leases, forwarded workflow runtime tools, cyclic workflow progression, intermediate output, final workflow output, and clean final projections.
- Workflow prompt-injection invariant: **pass**. Workflow prompt construction is scheduler-owned and shared by local dispatch, remote dispatch, retry/replay paths, and tests; the renderer injects turn index, last-turn guidance only on the last allowed node turn, final-output tool guidance, runtime-tool instructions, handoff payloads, edge contracts, and control mailbox content.
- Local MCP/skill management: **strict local pass**. On 2026-04-18, `node apps/cli/scripts/live-mcp-skill-drill.mjs --provider codex --require-web-skill --live-mcp-use --timeout-ms 300000 --poll-ms 1000` passed in ~40s, and `node apps/cli/scripts/live-mcp-skill-drill.mjs --provider opencode --provider-model opencode=opencode/gpt-5.2 --require-web-skill --live-mcp-use --timeout-ms 300000 --poll-ms 1000` passed in ~35s. Both installed real Playwright MCP, installed public `vercel-labs/agent-skills` skill `deploy-to-vercel`, installed the deterministic drill skill, verified pre-granted skill use, verified same-turn `request_extension` returns a skill body followed immediately, restarted the idle provider process for next-launch MCP rendering, verified a provider-native Playwright/browser tool call, and wrote the Playwright marker through Chariox workspace live sync. GitHub MCP remains optional and is skipped unless `GITHUB_PERSONAL_ACCESS_TOKEN` or `GITHUB_TOKEN` is present with `--include-github-mcp`.
- Remote skill management: **pass**. On 2026-04-18, `node apps/cli/scripts/live-remote-skill-drill.mjs --provider opencode --model opencode/gpt-5.2 --effort low --timeout-ms 300000 --poll-ms 1000` and `node apps/cli/scripts/live-remote-skill-drill.mjs --provider codex --model gpt-5.2 --effort low --timeout-ms 300000 --poll-ms 1000` passed. Both created isolated relay/home/worker daemons, installed a Chariox-owned skill with an asset, verified local-only grants do not materialize on the worker, moved a pre-granted local agent to remote and verified grant synchronization, verified remote-first grant-time materialization under `.chariox/remote/skills/...`, deleted the worker materialization and verified prompt-time repair, verified same-turn remote `request_extension` materialization, submitted live remote prompts, and verified provider output file `outputs/remote-skill-provider.txt` contained the synchronized asset token plus `REMOTE_SKILL_DRILL_OK`.
- Remote home-owned extensions: **pass**. On 2026-05-29, `node apps/cli/scripts/live-remote-home-extension-drill.mjs`, `node apps/cli/scripts/live-remote-home-extension-drill.mjs --collab`, `node apps/cli/scripts/live-remote-home-extension-drill.mjs --hetzner-worker`, and `node apps/cli/scripts/live-remote-home-extension-drill.mjs --hetzner-worker --collab` passed before and after the drill-library refactor. On 2026-05-29, after home-extension audit/observability and TUI/web audit-detail hardening, the local single-user/collab drills passed again, the Hetzner checkout was fast-forwarded to `d7d4c65d` and rebuilt, and both Hetzner single-user/collab drills passed again. After shared script timeout/output-limit policy and protocol version `70`, the local single-user/collab drills passed again, the Hetzner checkout was fast-forwarded to `d90c22e66166ff0233c93032ddb864002b0964a3` and rebuilt, and both Hetzner single-user/collab drills passed again. After invocation replay/cancellation/result-size policy tests, the local single-user/collab drills passed again, the Hetzner checkout was fast-forwarded to `c532e3fd` and rebuilt with the rustup Cargo toolchain, both Hetzner single-user/collab drills passed again, and hosted Cloud second-kernel drills passed for single-user and `--multi-user` collab via `pnpm run smoke:hosted-oss-drill -- --second-kernel` and `pnpm run smoke:hosted-oss-drill -- --second-kernel --multi-user`. On 2026-06-04, after scoping Hetzner drill relay cleanup to the drill-owned pid file/root, `node apps/cli/scripts/live-remote-home-extension-matrix-drill.mjs --include-hetzner --only hetzner-single` passed in ~19s and `node apps/cli/scripts/live-remote-home-extension-matrix-drill.mjs --include-hetzner --only hetzner-collab` passed in ~18s. The same day, hosted Cloud second-kernel staging passed with `pnpm run smoke:hosted-oss-drill -- --second-kernel` and `pnpm run smoke:hosted-oss-drill -- --second-kernel --multi-user`, covering `https://chariox-cloud-staging.osc-fr1.scalingo.io` plus the hosted `wss://195.201.123.115.sslip.io` relay. After no-placeholder remote recovery hardening at `8a8fb723`, `node apps/cli/scripts/live-remote-home-extension-matrix-drill.mjs` passed local self-hosted single-user and collab again; after recording that evidence at `45ddea5b`, `node apps/cli/scripts/live-remote-home-extension-matrix-drill.mjs --include-hetzner --only hetzner-single,hetzner-collab --continue-on-failure` passed current Hetzner single-user and collab again. The hosted Cloud second-kernel drills were then rerun against staging and passed for single-user and `--multi-user`; the multi-user run retried one transient `ECONNRESET`, then completed the collab home-extension check and Cloud invite acceptance. The drills verified home-owned script, MCP, and connector manifests projected to the worker, execution stayed on home, worker credentials/definitions were not required, revoke removed worker advertisement and blocked stale calls, worker-local MCP name collision failed before launch, hosted relay routing stayed on `wss://195.201.123.115.sslip.io`, and in scoped-relay/Cloud collaboration the peer owned the remote agent but could not grant, revoke, or request home-owned extensions.
- Remote home-owned extensions revalidation: **pass**. On 2026-06-13, after protocol `138` and TUI/web waiting-room home-proxy blocker visibility, `node apps/cli/scripts/live-remote-home-extension-matrix-drill.mjs --continue-on-failure` passed local self-hosted single-user and collab; `node apps/cli/scripts/live-remote-home-extension-matrix-drill.mjs --include-hetzner --only hetzner-single,hetzner-collab --continue-on-failure` passed Hetzner single-user and collab; and staging hosted Cloud second-kernel passed with `pnpm run smoke:hosted-oss-drill -- --second-kernel` plus `pnpm run smoke:hosted-oss-drill -- --second-kernel --multi-user`, including the collab home-extension check over `wss://195.201.123.115.sslip.io`.
- Remote MCP management: **strict pass**. On 2026-04-18, after production HTTPS/chunked MCP proxy support landed, `node apps/cli/scripts/live-remote-mcp-drill.mjs --provider opencode --model opencode/gpt-5.2 --effort low --timeout-ms 300000 --live-mcp-use` passed in ~31s and `node apps/cli/scripts/live-remote-mcp-drill.mjs --provider codex --model gpt-5.2 --effort low --timeout-ms 300000 --live-mcp-use` passed in ~79s. Both created isolated relay/home/worker daemons with separate `HOME` roots, verified worker-missing MCP fails fast, verified worker global definition mismatch fails fast, verified worker project-local matching MCP overrides a mismatched global MCP, verified missing worker stdio command fails fast, verified missing worker env var fails fast, verified a provider-native remote Playwright/browser MCP tool call on the worker, and wrote `outputs/remote-playwright-mcp.txt` with `M7_REMOTE_PLAYWRIGHT_MCP_OK` through Chariox workspace live sync. OpenCode used `playwright_browser_snapshot`; Codex used `browser_tabs`.
- Workflow MCP grants: **pass**. On 2026-04-19, `node apps/cli/scripts/live-workflow-runtime-drill.mjs --spawn-daemon --scenario mcp-echo-workflow --providers opencode --provider-model opencode=opencode/gpt-5.2 --poll-limit 180 --poll-interval-ms 1000` passed in ~14s, and `node apps/cli/scripts/live-workflow-runtime-drill.mjs --spawn-daemon --scenario mcp-echo-workflow --providers codex --provider-model codex=gpt-5.2 --poll-limit 180 --poll-interval-ms 1000` passed in ~14s. Both installed deterministic MCP `workflow_echo`, granted it to the workflow agent, completed a provider-native echo MCP call, wrote an exact workspace live sync marker, and submitted final workflow output `{"echo":"ECHO:M7_WORKFLOW_ECHO_OK"}`. Remote OpenCode passed with `node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario mcp-echo-workflow --provider opencode --model gpt-5.2 --provider-model opencode=opencode/gpt-5.2 --poll-limit 240 --poll-interval-ms 1000` in ~72s. Remote Codex passed with `node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario mcp-echo-workflow --provider codex --model gpt-5.2 --provider-model codex=gpt-5.2 --poll-limit 240 --poll-interval-ms 1000` in ~116s. The earlier remote Codex failure was a drill invocation error: without the explicit provider override, the workflow drill mapped bare `gpt-5.2` to `gpt-5.2-codex`, and Codex logs showed that model is rejected by ChatGPT-backed Codex accounts.

Point-2 fixes proven by the local workflow catalog:

- Workflow endpoint invocation now enqueues the entry dispatches returned by owned invoke admission instead of dropping them.
- Owned workflow prompt rendering now includes workflow prompt, node instructions, outgoing edge contracts, ack/validation/tool instructions, and the final fenced JSON contract for entry and downstream turns.
- Terminal pumping drains all starting/running provider runs in the session, not only the focused active provider.
- Structured provider lifecycle handling ignores stale completion/idle events and requires a submitted prompt before treating idle as completion.
- Workflow console writes no longer deadlock by taking a session-store read while constructing arguments for a write-locked mutation.
- Resolved missing-output/output-validation failures are cleared when a later successful turn for the same workflow node produces output or submits final output.
- Node max-turn budget accounting counts completed turns with completion payloads, so recovered missing-output attempts do not consume the production success budget.
- Intermediate-output runtime tools now enqueue the downstream prompt dispatches they prepare; the previous path created downstream node runs but dropped the async provider submission work.
- Final workflow-output submissions no longer generate downstream handoff validation failures while completion is still committing pending turn outputs.
- The budgeted cyclic final-output scenario now checks the exact final output payload, so a stale pre-ack provider output cannot accidentally satisfy a later final workflow turn.
- Conditional branching drill: **pass**. A router node with two outgoing edges emitted a structured `workflow_handoffs` payload selecting only the even branch; the selected branch completed with `{"bucket":"even","values":[2]}` and the excluded branch was not invoked.
- Immediate-release drill: **pass for release semantics**. A valid intermediate output `{"value":1842}` created and prompted the downstream node while the producer node was still running, proving asynchronous handoff release before producer turn completion. Provider settlement after the intermediate tool remains a separate follow-up hardening item.

Point-3 evidence:

```
node apps/cli/scripts/live-remote-multi-agent-relay-drill.mjs --providers opencode,codex --model gpt-5.2 --timeout-ms 240000 --poll-ms 1000
```

Observed result: both providers were visible on the worker machine, both providers had local sidecar agents and remote leased worker agents, both local and relayed clients observed four assistant completions, the relayed client observed `transport_closed` and `transport_resumed`, and both remote worker agents completed prompts after relay restart.

Point-4 evidence:

```
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario validated-increment-chain --providers opencode,codex --model gpt-5.2 --poll-limit 600 --poll-interval-ms 250
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario simple-chain --providers opencode,codex --model gpt-5.2 --poll-limit 600 --poll-interval-ms 250
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario console-increment-chain --providers opencode,codex --model gpt-5.2 --poll-limit 600 --poll-interval-ms 250
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario final-run-output-chain --providers opencode,codex --model gpt-5.2 --poll-limit 600 --poll-interval-ms 250
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario cyclic-final-run-output-chain --providers opencode,codex --model gpt-5.2 --poll-limit 600 --poll-interval-ms 250
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario cyclic-budgeted-final-run-output-chain --providers opencode,codex --model gpt-5.2 --poll-limit 600 --poll-interval-ms 250
node apps/cli/scripts/live-remote-workflow-runtime-drill.mjs --scenario cyclic-final-run-with-intermediate-output-chain --providers opencode,codex --model gpt-5.2 --poll-limit 700 --poll-interval-ms 250
```

Observed result: the remote workflow catalog completed through the relay with worker-machine leased agents. The closing cyclic intermediate-output run completed with validated intermediate output and final workflow output from `workflow-node-run-2`, final output `{"value":1843}`, no failure events, and status `Completed`.
